import React from 'react';
import SectionRaces from '../components/SectionRaces';

const Races = () => {

    return (
        <>
            <SectionRaces/>
        </>
    );
};

export default Races;
