import styled from 'styled-components';

export const NavStyle = styled.header `

    header {
        width: 100%;
        background-color: #EDEDED;
    }

    #nav-mobile {
        display:none;
    }

`
